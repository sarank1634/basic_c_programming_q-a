# basic_c_programming_q-a

16-nov-20225

1. Swap two variables.
2. use forloop print 0 to 100 numbers.
3. using forloop print(13,16,19,....1313).
4. Take start and end as input from terminal and do for loop increment = 10, (start=20, end=408).
5. Take a number as input from terminal and print tabels from 1 to 20 for that number.
6. Take age as input and print if the person is major or not
7. Take a number as input and if that number is ODD or Even.
8. Take name,city and age as input and print (my name is { name} live in {city} am {age} years old.
9. Take a number as inout if it is divisible by 3 and 5 print "Good number" else print "Bad Number".
10. calculate ricksaw cost, take distance as input of distance is less than 3, then fair is fixed 5rs. else for each km after 3rd km, the Price will increase by 2rsof dist.
    (5+(7-3)*2=5+(8)=13 rs.
